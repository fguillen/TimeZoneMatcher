import { timeZoneTdClass } from 'time-zones-matcher/helpers/time-zone-td-class';
import { module, test } from 'qunit';

module('Unit | Helper | time zone td class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeZoneTdClass([42]);
  assert.ok(result);
});
